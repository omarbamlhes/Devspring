{{ drupal_block('devspring_subheader_block') }}
    <div class="container-xxl py-5 bg-primary hero-header mb-5">
                <div class="container my-5 py-5 px-lg-5">
                    <div class="row g-5 py-5">
                        <div class="col-12 text-center text-white">
                        {{ breadcrumb }}
                        {{ "Consultation"|t }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
        

<div class="container-xxl py-5 consultation-form">
            <div class="container px-lg-5">
                <div class="row justify-content-center">
                    <div class="col-lg-7">
                        <div class="section-title position-relative text-center mb-5 pb-2 wow fadeInUp" data-wow-delay="0.1s">
                            <h6 class="position-relative d-inline text-primary ps-4">Consultation</h6>
                            <h2 class="mt-2">Request a Consultation</h2>
                        </div>
                        <div class="wow fadeInUp" data-wow-delay="0.3s">
                        <form {{ attributes }}>
                            <div class="row g-3">

  {{ title_prefix }}
                        {{ children }}
                        {{ title_suffix }}
                        </div>
                        </form>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

{{ drupal_block('devspring_information_block') }}
